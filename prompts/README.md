# 审计提示词

本目录存放只读源码审计用的 system / task prompts。

## 护栏要求（实现时必须遵守）

1. 禁止输出可直接对线上目标执行的 exploit 步骤。  
2. 发现项格式：类别、严重度、文件路径、行号/符号、说明、修复建议。  
3. 不确定时标注「需人工确认」，不得伪装成已验证可利用。  
4. 不要求、不调用对外部 URL 的请求类工具。  

具体 prompt 文件将在引擎落地时加入。
